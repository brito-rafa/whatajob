---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: billybobs.whatajob.io
spec:
  group: whatajob.io
  names:
    kind: Billybob
    listKind: BillybobList
    plural: billybobs
    singular: billybob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Billybob is the Schema for the billybobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BillybobSpec defines the desired state of Billybob
            properties:
              numberPods:
                type: integer
            type: object
          status:
            description: BillybobStatus defines the observed state of Billybob
            properties:
              currentPods:
                type: integer
              podNames:
                items:
                  type: string
                type: array
              selector:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.numberPods
        statusReplicasPath: .status.currentPods
      status: {}
